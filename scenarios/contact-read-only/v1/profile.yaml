profile:
  id: scenario_contact_read_only_v1
  description: Sanitized contact canary for scenario-runner qualification

datasets:
  - name: contacts
    source:
      file: contacts.csv
    target:
      model: res.partner
      mode: upsert
    source_identity:
      fields: [reference]
    target_identity:
      components:
        - source_fields: [reference]
          target_fields: [ref]
          type: string
          normalize: {trim: true}
    fields:
      name:
        source: name
        type: string
        required: true
        required_on_create: true
        compare: true
        normalize: {trim: true, collapse_whitespace: true}
    relations: {}
